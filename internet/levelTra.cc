#include<iostream>
#include <vector>

using namespace std;
typedef vector<vector<int> > VVInt;
typedef vector<int> VInt;

class Solution {
public:
    VVInt levelReverseTra(TreeNode* root) {
        // Note: The Solution object is instantiated only once and is reused by each test case.
        
    }

    
    VVInt vvi; 
};



